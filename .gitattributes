# Keep line endings consistent between Windows (Cursor) and GitHub.
# Without this, every file looks "modified" in git after a Windows checkout.
* text=auto eol=lf
*.png binary
*.jpg binary
*.jpeg binary
*.svg text eol=lf
