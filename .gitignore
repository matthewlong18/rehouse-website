# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Logs & dependencies (not used yet, here for later)
node_modules/
npm-debug.log*
*.log

# Local environment / secrets — never commit these
.env
.env.local

# Transfer archive from the Claude build — safe to delete
_rehouse-build.zip
_*.zip
_to_delete/
